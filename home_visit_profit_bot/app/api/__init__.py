"""FastAPI-слой (Фаза 1). Заменяет ThreadingHTTPServer из app/location_api.py.

Хендлеры остаются СИНХРОННЫМИ (def, не async def): FastAPI гоняет их в пуле потоков,
и это позволяет переиспользовать весь проверенный синхронный код сервисов и
репозиториев без переписывания на async — то есть без риска, что расчёт на новом
стеке разъедется со старым. Масштаб обеспечивают пул соединений (app/pool.py) и
лимиты uvicorn, а не async ради async.
"""

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
        Микрофон (Ф14.4) — ТОЛЬКО для голосового ввода адреса на телефонах БЕЗ
        Google-сервисов (нет системного распознавателя): короткая запись уходит на наш
        ASR по шифрованному каналу. На телефонах с Google используется системный
        распознаватель без этого разрешения. Запись живёт секунды в кеше и удаляется.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
        Начиная с Android 11 приложение не видит чужие пакеты без объявления. Без этого
        блока проверка «а установлен ли Яндекс?» всегда отвечала бы «нет», и кнопка
        «Поехали» молча уходила бы в запасной вариант.
    -->
    <queries>
        <package android:name="ru.yandex.yandexnavi" />
        <package android:name="ru.yandex.yandexmaps" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
                Share-target (Фаза 15.1): принимаем «Поделиться» текстом из любого
                мессенджера — самый короткий путь заказа в систему. Текст разбирается
                пакетным парсером и показывается на экране подтверждения.
            -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <!--
                Share-target скриншотом (Фаза 15.4): принимаем «Поделиться» картинкой —
                скриншот списка заказов из мессенджера уходит на наш OCR (сервер 2, канал
                WireGuard), распознанный текст — тем же пакетным парсером на экран
                подтверждения. On-device OCR кириллицы невозможен (ML Kit не умеет, Ф15.3).
            -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <service
            android:name=".LocationUploadService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <receiver
            android:name=".StopActionReceiver"
            android:exported="false" />
    </application>
</manifest>
